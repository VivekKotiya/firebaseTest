import { getPosts, putPost, updatePost } from "./fetch";
import { getAxiosRequest, postAxiosRequest, putAxiosRequest, patchAxiosRequest } from './axios';
export { 
    getPosts, 
    putPost, 
    updatePost, 
    getAxiosRequest, 
    postAxiosRequest,
    putAxiosRequest,
    patchAxiosRequest
};